{
	"private": true,
	"name": "sayhi-bot",
	"version": "0.0.0",
	"type": "module",
	"module": "dist/index.mjs",
	"scripts": {
		"deploy": "wrangler publish",
		"build": "node --no-warnings --experimental-vm-modules scripts/build.js",
		"dev:deploy": "node --no-warnings --experimental-vm-modules scripts/deploy.js"
	},
	"dependencies": {
		"slshx": "^0.3.0"
	},
	"devDependencies": {
		"esbuild": "^0.16.7",
		"wrangler": "^2.6.2"
	}
}
